{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Keycloak": {
    "//": "The realm a token must come from and the audience it must carry. Both are contracts with FieldKit.AppHost/realms/fieldkit-dev-realm.json — change one and you must change the other. Realm-per-tenant means Realm becomes per-request rather than fixed once IAM lands (ADR-0008, multi-issuer validation).",
    "Realm": "fieldkit-dev",
    "Audience": "fieldkit-api"
  }
}
