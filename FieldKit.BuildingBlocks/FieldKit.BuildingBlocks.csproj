<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\FieldKit.SharedKernel\FieldKit.SharedKernel.csproj" />
  </ItemGroup>

  <!-- The banned-API analyzer, its symbol lists and the RS0030-as-error escalation now come from
       Directory.Build.props, so every project gets them rather than just this one. -->

</Project>
